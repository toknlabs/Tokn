# Secrets — never commit. The Appwrite API key grants full project access.
.env
.env.*
!.env.example

node_modules/
dist/
.test-dist/
.next/
out/

# Local databases and scratch data
data/
*.db
*.db-wal
*.db-shm

.DS_Store
*.log

# Brand assets and the script that generates them are kept locally, not in
# the repository. `brand/.fonts/` inside it holds fonts fetched from Google
# Fonts, which are not ours to redistribute either way.
brand/

# Editor, agent and tooling state — local to a machine, not part of the project.
.claude/
.mcp.json
.eaonbrain/
CLAUDE.md
.vscode/
.idea/
.vercel
.env*
